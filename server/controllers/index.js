import users from './users';
import groups from './groups';
import messages from './messages';

/**
 * @fileOverview controllers reusable API module that allows us
 * creating users, loging in users, 
 * creating groups, adding users to group
 * and posting messages to groups.
 *
 * @exports users
 * @exports groups
 * @exports groups
 */
module.exports = {
  users, groups, messages,
};
